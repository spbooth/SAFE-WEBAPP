# locking does not work with memory tables
create_table.use_memory=false
service.feature.database_transactions.serialized=on
service.feature.database_transactions=on
# needed but too late to set this in fixture
# so also set in test.properties
transaction.isolation_level=SERIALIZABLE