/** Classes to store config data in the database.
 * 
 * As the database already depends on the config package
 * we place this in the jdbc tree to prevent a circular dependency.

 * 
 * @author spb
 *
 */
package uk.ac.ed.epcc.webapp.jdbc.config;