package uk.ac.ed.epcc.webapp.session.perms;
/** Classes to support building relationship filter.
 * As the specifications need to be converted into different results the specification
 * is parsed into and Abstract-Syntax-Tree then converted using the visitor pattern.
 */
