<%
	String privacy_policy=conn.getExpandedProperty("service.url.privacypolicy");
    if( privacy_policy != null && ! privacy_policy.isEmpty() ){ 
%>
<p>
<small>All information supplied is held and processed in accordance with our <a href="<%= privacy_policy%>" target="_blank">Personal Data and Privacy Policy</a>.</small>
</p>
<% } %>